<!doctype html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تطبيق دلال المتكامل</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700;800&display=swap" rel="stylesheet">
    <style>
        * { font-family: 'Tajawal', sans-serif; transition: all 0.3s ease; }
        body { background-color: #0f172a; color: white; margin: 0; padding: 0; overflow-x: hidden; }
        .glass-card { background: rgba(255, 255, 255, 0.08); backdrop-filter: blur(15px); border: 1px solid rgba(255, 255, 255, 0.1); }
        .page { display: none; min-height: 100vh; padding: 20px; padding-bottom: 80px; }
        .page.active { display: block; animation: fadeIn 0.5s ease; }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
        .input-field { background: rgba(255, 255, 255, 0.05); border: 1px solid rgba(255, 255, 255, 0.15); color: white; width: 100%; padding: 12px; border-radius: 12px; }
        .nav-bar { position: fixed; bottom: 0; left: 0; right: 0; background: #1e293b; border-top: 1px solid #334155; display: flex; justify-content: space-around; padding: 10px; z-index: 100; }
    </style>
</head>
<body>

    <div id="login-page" class="page active flex flex-col items-center justify-center">
        <div class="text-center mb-10">
            <div class="w-20 h-20 bg-orange-600 rounded-3xl mx-auto flex items-center justify-center shadow-2xl mb-4">
                <span class="text-4xl">🏗️</span>
            </div>
            <h1 class="text-3xl font-black text-orange-500">دلال</h1>
            <p class="text-slate-400 mt-2">بوابتك الموثوقة لسوق البناء</p>
        </div>
        
        <div class="glass-card w-full max-w-sm p-6 rounded-3xl space-y-4">
            <input type="tel" class="input-field" placeholder="رقم الجوال (7XXXXXXXX)">
            <button onclick="showPage('market-page')" class="w-full bg-orange-600 py-4 rounded-xl font-bold text-lg shadow-lg active:scale-95">دخول للمنصة</button>
            <p class="text-center text-xs text-slate-500">بالدخول أنت توافق على ميثاق الأمان المالي</p>
        </div>
    </div>

    <div id="market-page" class="page">
        <div class="flex justify-between items-center mb-6">
            <h2 class="text-2xl font-bold">بورصة دلال 📈</h2>
            <span class="bg-emerald-500/20 text-emerald-400 px-3 py-1 rounded-full text-xs">مباشر</span>
        </div>

        <div class="space-y-4">
            <div class="glass-card p-4 rounded-2xl border-r-4 border-orange-500">
                <div class="flex justify-between items-center mb-3">
                    <span class="font-bold">حديد تسليح (12 ملم)</span>
                    <span class="text-orange-500 font-black">$920/طن</span>
                </div>
                <div class="flex gap-2 text-xs">
                    <a href="https://wa.me/967XXXXXXXXX?text=طلب+فحص+حديد" class="flex-1 bg-orange-600 text-center py-2 rounded-lg font-bold">طلب فحص دلال</a>
                    <button class="flex-1 bg-slate-700 py-2 rounded-lg">التفاصيل</button>
                </div>
            </div>

            <div class="glass-card p-4 rounded-2xl border-r-4 border-amber-500">
                <div class="flex justify-between items-center mb-3">
                    <span class="font-bold">خشب ميرانتي (أحمر)</span>
                    <span class="text-amber-500 font-black">$450/متر</span>
                </div>
                <div class="flex gap-2 text-xs">
                    <a href="https://wa.me/967XXXXXXXXX?text=طلب+فحص+خشب" class="flex-1 bg-orange-600 text-center py-2 rounded-lg font-bold">طلب فحص دلال</a>
                    <button class="flex-1 bg-slate-700 py-2 rounded-lg">التفاصيل</button>
                </div>
            </div>
        </div>
    </div>

    <div id="calc-page" class="page">
        <h2 class="text-2xl font-bold mb-6 text-center text-orange-500">حاسبة الكميات الذكية 🧮</h2>
        <div class="glass-card p-6 rounded-3xl space-y-4">
            <div>
                <label class="block text-slate-400 mb-2">مساحة الأرض</label>
                <input type="number" id="area" class="input-field" placeholder="بالمتر المربع">
            </div>
            <div>
                <label class="block text-slate-400 mb-2">عدد الأدوار</label>
                <input type="number" id="floors" class="input-field" placeholder="عدد الطوابق">
            </div>
            <button onclick="calculate()" class="w-full bg-orange-600 py-4 rounded-xl font-bold mt-4 shadow-lg">احسب الاحتياج</button>
        </div>

        <div id="results" class="hidden mt-6 space-y-3">
            <div class="bg-slate-800 p-4 rounded-2xl flex justify-between items-center border border-orange-500/30">
                <span class="text-slate-400">الحديد المطلوب:</span>
                <span id="steel-res" class="text-xl font-bold text-orange-500">0 طن</span>
            </div>
            <div class="bg-slate-800 p-4 rounded-2xl flex justify-between items-center border border-amber-500/30">
                <span class="text-slate-400">الخشب المطلوب:</span>
                <span id="wood-res" class="text-xl font-bold text-amber-500">0 م³</span>
            </div>
        </div>
    </div>

    <div id="bottom-nav" class="nav-bar hidden">
        <div onclick="showPage('market-page')" class="flex flex-col items-center">
            <span class="text-xl">📊</span>
            <span class="text-[10px]">البورصة</span>
        </div>
        <div onclick="showPage('calc-page')" class="flex flex-col items-center">
            <span class="text-xl">🧮</span>
            <span class="text-[10px]">الحاسبة</span>
        </div>
        <div class="flex flex-col items-center text-slate-500">
            <span class="text-xl">🚛</span>
            <span class="text-[10px]">الشحن</span>
        </div>
        <div onclick="showPage('login-page')" class="flex flex-col items-center text-slate-500">
            <span class="text-xl">👤</span>
            <span class="text-[10px]">خروج</span>
        </div>
    </div>

    <script>
        // وظيفة التنقل بين الصفحات
        function showPage(pageId) {
            // إخفاء كل الصفحات
            document.querySelectorAll('.page').forEach(page => page.classList.remove('active'));
            // إظهار الصفحة المطلوبة
            document.getElementById(pageId).classList.add('active');
            
            // إخفاء أو إظهار شريط التنقل (يظهر فقط بعد تسجيل الدخول)
            if (pageId === 'login-page') {
                document.getElementById('bottom-nav').classList.add('hidden');
            } else {
                document.getElementById('bottom-nav').classList.remove('hidden');
            }
        }

        // وظيفة الحاسبة
        function calculate() {
            const area = document.getElementById('area').value;
            const floors = document.getElementById('floors').value;
            if (area > 0 && floors > 0) {
                document.getElementById('steel-res').innerText = (area * floors * 0.12).toFixed(2) + " طن";
                document.getElementById('wood-res').innerText = (area * floors * 0.025).toFixed(2) + " م³";
                document.getElementById('results').classList.remove('hidden');
            } else {
                alert("يرجى ملء البيانات");
            }
        }
    </script>

</body>
</html>